# Seeded fixture — a count stated in prose

From a closeout summary:

> "We scanned 202 commits and found 0 reconciliation gaps; the ledger is clean."

The `202` and `0` are asserted in prose — there is **no** fenced `reconcile` block recomputing
either number from the git log or the ledger. The count is unverifiable as written (the
"202 scanned / 0 corrections" assurance-theater class).
