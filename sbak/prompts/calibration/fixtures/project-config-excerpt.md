# project-config.md (excerpt) - M06 restore capability

```yaml
risk_triggers: []
```

From the M06.B stage scope:

> Implements `restore --from backup.sql`: drops the live schema and reloads it
> from the backup file, replacing current user data.
