#!/usr/bin/env sh
# @kit-version 0.1.0-dev
# .githooks/pre-push  —  the full local matrix (Linux-in-Docker + native).
# Blocks the push on any failure. This is what replaces the per-push cloud test
# matrix under verification_locus: local_first | hybrid.
#
# Activated by: git config core.hooksPath .githooks
#   (run `node scripts/install-hooks.cjs` once after cloning).
# Emergency override: git push --no-verify  (logged, never routine; it cannot
# bypass the required PR check — see docs/gates.md).
set -e
node scripts/verify-local.cjs
